Financial Suite (Loan & Expense Tracker)A modular Python application to manage user authentication, age validation, and financial health checks.## Project Structuremain.py: The entry point/menu system.validators.py: Handles login and age verification logic.expense_tracker.py: Analyzes spending relative to income.loan_app.py: Evaluates loan eligibility based on Debt-to-Income (DTI).## FeaturesSecure Gatekeeping: Validates user credentials and age ($\ge 18$).Smart Tracking: Flags overspending if expenses exceed 50% of income.Loan Assessment: Checks if the debt-to-income ratio is within the 40% threshold.## How to RunPrepare Files: Ensure all .py files are in the same directory.Execute: Run the following command in your terminal:Bashpython3 main.py
Credentials: Use default login:Username: flexsafPassword: flexsaf2026## Logic OverviewThe apps use the following logic to determine financial status:AppThresholdAlert TriggerExpense Tracker$50\%$ of Income"Overspending" warning if exceeded.Loan Checker$40\%$ of Income"Ineligible" if expenses are higher.
